#ifndef C_POET_H
#define C_POET_H

#define PROJECT_NAME "${PROJECT_NAME}"
#define APP_NAME "${APP_NAME}"
#define APP_URL "${APP_URL}"
#define APP_VERSION_X ${APP_VERSION_X}
#define APP_VERSION_Y ${APP_VERSION_Y}
#define APP_VERSION_Z ${APP_VERSION_Z}
#define APP_VERSION_E "${APP_VERSION_E}"
#define APP_VERSION "V${APP_VERSION_X}.${APP_VERSION_Y}.${APP_VERSION_Z}.${APP_VERSION_E}"
#define APP_AUTHORS "${APP_AUTHORS}"
#define APP_BUILD_TIME "${APP_BUILD_TIME}"
#define APP_BUILD_PLATFORM "${APP_BUILD_PLATFORM}"

#endif
